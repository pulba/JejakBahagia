---
import Archive from '../layouts/archive.astro';
import '@fontsource-variable/josefin-sans';
import { Icon } from 'astro-iconify';
const posts = await Astro.glob('../pages/posts/*.md');
---

<Archive title="Tema" desc="Dengan adanya preset kamu tinggal pilih design undangan sesuai dengan yang kamu mau!">

  <section>
    <div class="container px-6 py-20 mx-auto text-center">
      <div class="space-y-2 max-w-xl mx-auto">
        <h1
          class="font-bold font-poppins text-4xl md:text-5xl text-pink-600"
        >
        Pilihan Desain
        </h1>
        <p class="mt-4 text-center text-gray-600 font-poppins mx-auto">
          Dengan adanya preset kamu tinggal pilih design undangan sesuai dengan yang kamu mau!
        </p>
      </div>
      <div class="mx-auto p-4 w-full" x-data="{ activeTab: 'tab1' }">
        <div class="tabs grid grid-cols-2 sm:grid-cols-3 gap-4 justify-center max-w-screen-sm mx-auto my-12 font-poppins *:rounded-md *:py-3 *:px-6">
          <button @click="activeTab = 'tab1'" :class="activeTab === 'tab1' ? 'bg-pink-600 text-white' : 'bg-gray-600 text-white'" class="tablinks">Basic</button>
          <button @click="activeTab = 'tab2'" :class="activeTab === 'tab2' ? 'bg-pink-600 text-white' : 'bg-gray-600 text-white'" class="tablinks">Silver</button>
          <button @click="activeTab = 'tab3'" :class="activeTab === 'tab3' ? 'bg-pink-600 text-white' : 'bg-gray-600 text-white'" class="tablinks">Diamond</button>
          <button @click="activeTab = 'tab4'" :class="activeTab === 'tab4' ? 'bg-pink-600 text-white' : 'bg-gray-600 text-white'" class="tablinks">Exclusive</button>
          <button @click="activeTab = 'tab5'" :class="activeTab === 'tab5' ? 'bg-pink-600 text-white' : 'bg-gray-600 text-white'" class="tablinks">Tanpa Foto</button>
          <button @click="activeTab = 'tab6'" :class="activeTab === 'tab6' ? 'bg-pink-600 text-white' : 'bg-gray-600 text-white'" class="tablinks">Birthday</button>
      </div>
      
    
        <div x-show="activeTab === 'tab1'" class="tabcontent">
          <div class="grid grid-cols-1 min-[451px]:grid-cols-2 lg:grid-cols-3 gap-x-4 gap-y-8 max-w-screen-lg mx-auto">
            {posts
              .filter((post) => post.frontmatter.basic)
              .slice(0, 15)
              .map((post) => (
                <div class="relative px-6 pt-4 pb-8 bg-white rounded-lg border-2">
                  <div class="relative">
                    <div class="overflow-hidden rounded-lg">
                      <div class="w-full object-cover">
                        <a href={post.frontmatter.slug}>
                          <img src={post.frontmatter.img.url} alt={post.frontmatter.desc} />
                        </a>
                      </div>
                    </div>
                    <p class="font-bold text-xl pt-4 text-gray-800 sm:text-left capitalize">{post.frontmatter.type}</p>
                    <div class="flex flex-row gap-2">
                      <p class="p-1 bg-pink-600 text-white font-bold">50%</p>
                      <p class="font-bold text-pink-600 line-through">{post.frontmatter.unprice}</p>
                    </div>
                    <p class="font-bold text-gray-800 sm:text-left text-lg">{post.frontmatter.price}</p>
                    <div class="flex flex-col min-[320px]:flex-row gap-4 items-center justify-center min-[320px]:justify-start min-[320px]:justify-between mt-2">
                      <a href={post.frontmatter.slug} class="flex flex-row items-center justify-between gap-2 text-white bg-gray-600 w-fit px-4 py-2 rounded-md">
                        {post.frontmatter.link} Preview <Icon name="mdi:eye" class="w-4 h-4" />
                      </a>
                      <a href="/pesan" class="flex flex-row items-center justify-between gap-2 text-white bg-pink-600 w-fit px-4 py-2 rounded-md">
                        Pesan <Icon name="mdi:whatsapp" class="w-4 h-4 text-white" />
                      </a>
                    </div>
                  </div>
                </div>
              ))}
          </div>
        </div>
    
        <div x-show="activeTab === 'tab2'" class="tabcontent">
          <div class="grid grid-cols-1 min-[451px]:grid-cols-2 lg:grid-cols-3 gap-x-4 gap-y-8 max-w-screen-lg mx-auto">
            {posts
              .filter((post) => post.frontmatter.silver)
              .slice(0, 15)
              .map((post) => (
                <div class="relative px-6 pt-4 pb-8 bg-white rounded-lg border-2">
                  <div class="relative">
                    <div class="overflow-hidden rounded-lg">
                      <div class="w-full object-cover">
                        <a href={post.frontmatter.slug}>
                          <img src={post.frontmatter.img.url} alt={post.frontmatter.desc} />
                        </a>
                      </div>
                    </div>
                    <p class="font-bold text-xl pt-4 text-gray-800 sm:text-left capitalize">{post.frontmatter.type}</p>
                    <div class="flex flex-row gap-2">
                      <p class="p-1 bg-pink-600 text-white font-bold">50%</p>
                      <p class="font-bold text-pink-600 line-through">{post.frontmatter.unprice}</p>
                    </div>
                    <p class="font-bold text-gray-800 sm:text-left text-lg">{post.frontmatter.price}</p>
                    <div class="flex flex-col min-[320px]:flex-row gap-4 items-center justify-center min-[320px]:justify-start min-[320px]:justify-between mt-2">
                      <a href={post.frontmatter.slug} class="flex flex-row items-center justify-between gap-2 text-white bg-gray-600 w-fit px-4 py-2 rounded-md">
                        {post.frontmatter.link} Preview <Icon name="mdi:eye" class="w-4 h-4" />
                      </a>
                      <a href="/pesan" class="flex flex-row items-center justify-between gap-2 text-white bg-pink-600 w-fit px-4 py-2 rounded-md">
                        Pesan <Icon name="mdi:whatsapp" class="w-4 h-4 text-white" />
                      </a>
                    </div>
                  </div>
                </div>
              ))}
          </div>
        </div>
    
        <div x-show="activeTab === 'tab3'" class="tabcontent">
          <div class="grid grid-cols-1 min-[451px]:grid-cols-2 lg:grid-cols-3 gap-x-4 gap-y-8 max-w-screen-lg mx-auto">
            {posts
              .filter((post) => post.frontmatter.diamond)
              .slice(0, 15)
              .map((post) => (
                <div class="relative px-6 pt-4 pb-8 bg-white rounded-lg border-2">
                  <div class="relative">
                    <div class="overflow-hidden rounded-lg">
                      <div class="w-full object-cover">
                        <a href={post.frontmatter.slug}>
                          <img src={post.frontmatter.img.url} alt={post.frontmatter.desc} />
                        </a>
                      </div>
                    </div>
                    <p class="font-bold text-xl pt-4 text-gray-800 sm:text-left capitalize">{post.frontmatter.type}</p>
                    <div class="flex flex-row gap-2">
                      <p class="p-1 bg-pink-600 text-white font-bold">50%</p>
                      <p class="font-bold text-pink-600 line-through">{post.frontmatter.unprice}</p>
                    </div>
                    <p class="font-bold text-gray-800 sm:text-left text-lg">{post.frontmatter.price}</p>
                    <div class="flex flex-col min-[320px]:flex-row gap-4 items-center justify-center min-[320px]:justify-start min-[320px]:justify-between mt-2">
                      <a href={post.frontmatter.slug} class="flex flex-row items-center justify-between gap-2 text-white bg-gray-600 w-fit px-4 py-2 rounded-md">
                        {post.frontmatter.link} Preview <Icon name="mdi:eye" class="w-4 h-4" />
                      </a>
                      <a href="/pesan" class="flex flex-row items-center justify-between gap-2 text-white bg-pink-600 w-fit px-4 py-2 rounded-md">
                        Pesan <Icon name="mdi:whatsapp" class="w-4 h-4 text-white" />
                      </a>
                    </div>
                  </div>
                </div>
              ))}
          </div>
        </div>
        <div x-show="activeTab === 'tab4'" class="tabcontent">
          <div class="grid grid-cols-1 min-[451px]:grid-cols-2 lg:grid-cols-3 gap-x-4 gap-y-8 max-w-screen-lg mx-auto">
            {posts
              .filter((post) => post.frontmatter.exclusive)
              .slice(0, 15)
              .map((post) => (
                <div class="relative px-6 pt-4 pb-8 bg-white rounded-lg border-2">
                  <div class="relative">
                    <div class="overflow-hidden rounded-lg">
                      <div class="w-full object-cover">
                        <a href={post.frontmatter.slug}>
                          <img src={post.frontmatter.img.url} alt={post.frontmatter.desc} />
                        </a>
                      </div>
                    </div>
                    <p class="font-bold text-xl pt-4 text-gray-800 sm:text-left capitalize">{post.frontmatter.type}</p>
                    <div class="flex flex-row gap-2">
                      <p class="p-1 bg-pink-600 text-white font-bold">50%</p>
                      <p class="font-bold text-pink-600 line-through">{post.frontmatter.unprice}</p>
                    </div>
                    <p class="font-bold text-gray-800 sm:text-left text-lg">{post.frontmatter.price}</p>
                    <div class="flex flex-col min-[320px]:flex-row gap-4 items-center justify-center min-[320px]:justify-start min-[320px]:justify-between mt-2">
                      <a href={post.frontmatter.slug} class="flex flex-row items-center justify-between gap-2 text-white bg-gray-600 w-fit px-4 py-2 rounded-md">
                        {post.frontmatter.link} Preview <Icon name="mdi:eye" class="w-4 h-4" />
                      </a>
                      <a href="/pesan" class="flex flex-row items-center justify-between gap-2 text-white bg-pink-600 w-fit px-4 py-2 rounded-md">
                        Pesan <Icon name="mdi:whatsapp" class="w-4 h-4 text-white" />
                      </a>
                    </div>
                  </div>
                </div>
              ))}
          </div>
        </div>
        <div x-show="activeTab === 'tab5'" class="tabcontent">
          <div class="grid grid-cols-1 min-[451px]:grid-cols-2 lg:grid-cols-3 gap-x-4 gap-y-8 max-w-screen-lg mx-auto">
            {posts
              .filter((post) => post.frontmatter.nofoto)
              .slice(0, 15)
              .map((post) => (
                <div class="relative px-6 pt-4 pb-8 bg-white rounded-lg border-2">
                  <div class="relative">
                    <div class="overflow-hidden rounded-lg">
                      <div class="w-full object-cover">
                        <a href={post.frontmatter.slug}>
                          <img src={post.frontmatter.img.url} alt={post.frontmatter.desc} />
                        </a>
                      </div>
                    </div>
                    <p class="font-bold text-xl pt-4 text-gray-800 sm:text-left capitalize">{post.frontmatter.type}</p>
                    <div class="flex flex-row gap-2">
                      <p class="p-1 bg-pink-600 text-white font-bold">50%</p>
                      <p class="font-bold text-pink-600 line-through">{post.frontmatter.unprice}</p>
                    </div>
                    <p class="font-bold text-gray-800 sm:text-left text-lg">{post.frontmatter.price}</p>
                    <div class="flex flex-col min-[320px]:flex-row gap-4 items-center justify-center min-[320px]:justify-start min-[320px]:justify-between mt-2">
                      <a href={post.frontmatter.slug} class="flex flex-row items-center justify-between gap-2 text-white bg-gray-600 w-fit px-4 py-2 rounded-md">
                        {post.frontmatter.link} Preview <Icon name="mdi:eye" class="w-4 h-4" />
                      </a>
                      <a href="/pesan" class="flex flex-row items-center justify-between gap-2 text-white bg-pink-600 w-fit px-4 py-2 rounded-md">
                        Pesan <Icon name="mdi:whatsapp" class="w-4 h-4 text-white" />
                      </a>
                    </div>
                  </div>
                </div>
              ))}
          </div>
        </div>
        <div x-show="activeTab === 'tab6'" class="tabcontent">
          <div class="grid grid-cols-1 min-[451px]:grid-cols-2 lg:grid-cols-3 gap-x-4 gap-y-8 max-w-screen-lg mx-auto">
            {posts
              .filter((post) => post.frontmatter.ultah)
              .slice(0, 15)
              .map((post) => (
                <div class="relative px-6 pt-4 pb-8 bg-white rounded-lg border-2">
                  <div class="relative">
                    <div class="overflow-hidden rounded-lg">
                      <div class="w-full object-cover">
                        <a href={post.frontmatter.slug}>
                          <img src={post.frontmatter.img.url} alt={post.frontmatter.desc} />
                        </a>
                      </div>
                    </div>
                    <p class="font-bold text-xl pt-4 text-gray-800 sm:text-left capitalize">{post.frontmatter.type}</p>
                    <div class="flex flex-row gap-2">
                      <p class="p-1 bg-pink-600 text-white font-bold">50%</p>
                      <p class="font-bold text-pink-600 line-through">{post.frontmatter.unprice}</p>
                    </div>
                    <p class="font-bold text-gray-800 sm:text-left text-lg">{post.frontmatter.price}</p>
                    <div class="flex flex-col min-[320px]:flex-row gap-4 items-center justify-center min-[320px]:justify-start min-[320px]:justify-between mt-2">
                      <a href={post.frontmatter.slug} class="flex flex-row items-center justify-between gap-2 text-white bg-gray-600 w-fit px-4 py-2 rounded-md">
                        {post.frontmatter.link} Preview <Icon name="mdi:eye" class="w-4 h-4" />
                      </a>
                      <a href="/pesan" class="flex flex-row items-center justify-between gap-2 text-white bg-pink-600 w-fit px-4 py-2 rounded-md">
                        Pesan <Icon name="mdi:whatsapp" class="w-4 h-4 text-white" />
                      </a>
                    </div>
                  </div>
                </div>
              ))}
          </div>
        </div>
      </div>
      
    </div>
  </section>
</Archive>
